# vim: set ai noet sw=4 ts=4:
# ==================================================================
# FILE     Makefile (for dwmstatus, toplevel)
# MACHINE  all
# INFO     dwm statusline in C
#		   -> only for quick make, not for install
# 		   for install: go to src/ folder and execute 
# 		   "sudo make clean install"

# DATE     31.03.2014
# OWNER    Bischofberger
# ==================================================================

DIR1=src

all: 
	cd ${DIR1}; make
